class Modelo {
  int id;
  String usuario;
  String nombre;
  String contrasena;
  String estado;
  String escolaridad;
  String habilidades;

  Modelo(this.id, this.usuario, this.nombre, this.contrasena, this.estado,
      this.escolaridad, this.habilidades);
}
